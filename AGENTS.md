# Custom GitHub Actions Runners

The goal of this project is to build custom GitHub Actions runners that resemble
the behaviour of the official runners.

Validate your changes by building the container image, and running commands
inside the container.

## Example how to build the image

```bash
docker build -t github-runners .
```
